# Managed by Ansible — mode 0600.
# ONLY for docker-compose ${...} interpolation (image pin + Traefik host rule).
# The container's runtime secrets live in data/.env (separate file, env_file).
HERMES_IMAGE={{ hermes_image }}
HERMES_HOST={{ hermes_host }}
