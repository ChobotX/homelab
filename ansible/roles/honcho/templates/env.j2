# Managed by Ansible — mode 0600.
# ONLY for docker-compose ${...} interpolation (image pin + pg password).
# The containers' runtime env lives in honcho.env (separate file, env_file).
HONCHO_IMAGE={{ honcho_image }}
POSTGRES_PASSWORD={{ honcho_db_password }}
