---
dependencies:
  - role: traefik
galaxy_info:
  role_name: syncthing
  author: homelab-admin
  description: Syncthing media ingest (behind Traefik, WG-only; sync protocol WG-bound)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [noble]
