---
- name: Swap hermes service
  ansible.builtin.command:
    chdir: /opt/hermes
    # --force-recreate so bind-mounted config/.env changes actually trigger a
    # restart (compose sees no service-spec diff otherwise).
    cmd: docker compose up -d --wait --no-deps --force-recreate hermes
  changed_when: true
