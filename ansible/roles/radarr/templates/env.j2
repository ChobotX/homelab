{# Managed by Ansible — mode 0600. #}
RADARR_IMAGE={{ radarr_image }}
RADARR_HOST={{ radarr_host }}
RADARR_PUID={{ radarr_puid }}
RADARR_PGID={{ radarr_pgid }}
RADARR_DOWNLOAD_ROOT={{ radarr_download_root }}
RADARR_MEDIA_ROOT={{ radarr_media_root }}
TZ={{ homelab_timezone | default('Etc/UTC') }}
